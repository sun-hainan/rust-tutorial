# 模块十四：数据结构与算法

## 学习目标
- 手写单向链表、双向链表
- 实现栈和队列（数组和链表两种方式）
- 掌握二叉搜索树（BST）的插入和查找
- 理解归并排序和快速排序原理
- 实现二分查找及变体
- 理解图算法（BFS/DFS）

---

## 1. 链表

### ASCII 图示

```
┌─────────────────────────────────────────────────────────┐
│              单向链表结构示意                            │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  LinkedList { head: Some(Node) }                       │
│                                                          │
│  head                                                    │
│   │                                                      │
│   ▼                                                      │
│  ┌──────┬─────┐    ┌──────┬─────┐    ┌──────┬─────┐   │
│  │ val:1│next │──→ │ val:2│next │──→ │ val:3│next │   │
│  └──────┴─────┘    └──────┴─────┘    └──────┴─────┘   │
│       Node             Node             Node             │
│                                                          │
│  push: 在头部插入 O(1)                                  │
│  pop:  从头部删除 O(1)                                  │
│  search: O(n)                                          │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 代码示例

```rust
fn linked_list() {
    #[derive(Debug)]
    struct Node<T> {
        value: T,
        next: Option<Box<Node<T>>>,
    }

    #[derive(Debug)]
    struct LinkedList<T> {
        head: Option<Box<Node<T>>>,
        size: usize,
    }

    impl<T> LinkedList<T> {
        fn new() -> Self { LinkedList { head: None, size: 0 } }

        fn push(&mut self, value: T) {
            let new_node = Box::new(Node { value, next: self.head.take() });
            self.head = Some(new_node);
            self.size += 1;
        }

        fn pop(&mut self) -> Option<T> {
            self.head.take().map(|node| {
                self.head = node.next;
                self.size -= 1;
                node.value
            })
        }

        fn len(&self) -> usize { self.size }
        fn is_empty(&self) -> bool { self.size == 0 }
    }

    let mut list = LinkedList::new();
    list.push(1);
    list.push(2);
    list.push(3);

    while let Some(v) = list.pop() {
        print!("{} ", v);
    }
    println!();

    // 反转链表（迭代）
    fn reverse_list(head: Option<Box<Node<i32>>>) -> Option<Box<Node<i32>>> {
        let mut prev = None;
        let mut current = head;
        while let Some(mut node) = current {
            current = node.next.take();
            node.next = prev;
            prev = Some(node);
        }
        prev
    }
}
```

---

## 2. 栈和队列

### 代码示例

```rust
fn stack_queue() {
    use std::collections::VecDeque;

    // 栈（用 Vec 模拟）
    let mut stack: Vec<i32> = vec![];
    stack.push(1);
    stack.push(2);
    stack.push(3);
    println!("stack pop = {:?}", stack.pop()); // Some(3)

    // 队列（用 VecDeque）
    let mut queue: VecDeque<i32> = VecDeque::new();
    queue.push_back(1);
    queue.push_back(2);
    queue.push_back(3);
    println!("queue front = {:?}", queue.pop_front()); // Some(1)

    // 括号匹配（栈应用）
    fn is_valid_parentheses(s: &str) -> bool {
        let mut stack = vec![];
        for c in s.chars() {
            match c {
                '(' | '[' | '{' => stack.push(c),
                ')' => { if stack.pop() != Some('(') { return false; } }
                ']' => { if stack.pop() != Some('[') { return false; } }
                '}' => { if stack.pop() != Some('{') { return false; } }
                _ => {}
            }
        }
        stack.is_empty()
    }
    println!("'()[]{{}}' valid = {}", is_valid_parentheses("()[]{}"));
}
```

---

## 3. 二叉搜索树（BST）

### ASCII 图示

```
┌─────────────────────────────────────────────────────────┐
│              二叉搜索树（BST）结构                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│                    5                                   │
│                   / \                                  │
│                  3   7                                 │
│                 / \   \                                 │
│                1   4   8                                 │
│                                                          │
│  性质：左子树所有节点 < 根节点 < 右子树所有节点           │
│                                                          │
│  查找 4: 5→3→4  O(log n)                               │
│  插入: 找到空位挂上                                       │
│  中序遍历: 1, 3, 4, 5, 7, 8（有序）                     │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 代码示例

```rust
fn tree_structures() {
    #[derive(Debug)]
    struct TreeNode<T> {
        value: T,
        left: Option<Box<TreeNode<T>>>,
        right: Option<Box<TreeNode<T>>>,
    }

    #[derive(Debug)]
    struct BST<T: std::cmp::Ord> {
        root: Option<Box<TreeNode<T>>>,
    }

    impl<T: std::cmp::Ord> BST<T> {
        fn new() -> Self { BST { root: None } }

        fn insert(&mut self, v: T) {
            match self.root.take() {
                None => self.root = Some(Box::new(TreeNode { value: v, left: None, right: None })),
                Some(mut node) => {
                    if v < node.value { node.left = Self::insert_into(node.left.take(), v); }
                    else if v > node.value { node.right = Self::insert_into(node.right.take(), v); }
                    self.root = Some(node);
                }
            }
        }

        fn insert_into(node: Option<Box<TreeNode<T>>>, v: T) -> Option<Box<TreeNode<T>>> {
            match node {
                None => Some(Box::new(TreeNode { value: v, left: None, right: None })),
                Some(mut n) => {
                    if v < n.value { n.left = Self::insert_into(n.left.take(), v); }
                    else if v > n.value { n.right = Self::insert_into(n.right.take(), v); }
                    Some(n)
                }
            }
        }

        fn search(&self, v: &T) -> bool {
            let mut current = self.root.as_deref();
            while let Some(node) = current {
                if v < &node.value { current = node.left.as_deref(); }
                else if v > &node.value { current = node.right.as_deref(); }
                else { return true; }
            }
            false
        }
    }

    let mut bst = BST::new();
    for v in [5, 3, 7, 1, 4, 6, 8] { bst.insert(v); }
    println!("search 4 = {}", bst.search(&4));
    println!("search 9 = {}", bst.search(&9));
}
```

---

## 4. 排序算法

### 代码示例

```rust
fn sorting_algorithms() {
    // 归并排序
    fn merge_sort<T: Ord + Clone>(arr: &[T]) -> Vec<T> {
        if arr.len() <= 1 { return arr.to_vec(); }
        let mid = arr.len() / 2;
        let left = merge_sort(&arr[..mid]);
        let right = merge_sort(&arr[mid..]);
        merge(&left, &right)
    }

    fn merge<T: Ord + Clone>(a: &[T], b: &[T]) -> Vec<T> {
        let mut result = Vec::with_capacity(a.len() + b.len());
        let mut i = 0; let mut j = 0;
        while i < a.len() && j < b.len() {
            if a[i] <= b[j] { result.push(a[i].clone()); i += 1; }
            else { result.push(b[j].clone()); j += 1; }
        }
        result.extend_from_slice(&a[i..]);
        result.extend_from_slice(&b[j..]);
        result
    }

    // 快速排序
    fn quick_sort<T: Ord + Clone>(arr: &[T]) -> Vec<T> {
        if arr.len() <= 1 { return arr.to_vec(); }
        let pivot = arr[0].clone();
        let (left, right) = arr[1:].partition(|x| x < &pivot);
        let mut result = quick_sort(&left);
        result.push(pivot);
        result.extend(quick_sort(&right));
        result
    }

    let data = vec![64, 34, 25, 12, 22, 11, 90];
    println!("merge sort: {:?}", merge_sort(&data));
    println!("quick sort: {:?}", quick_sort(&data));
}
```

---

## 5. 二分查找

### 代码示例

```rust
fn binary_search() {
    fn binary_search<T: Ord>(arr: &[T], target: &T) -> Option<usize> {
        let mut left = 0;
        let mut right = arr.len();

        while left < right {
            let mid = left + (right - left) / 2;
            if arr[mid] < *target { left = mid + 1; }
            else if arr[mid] > *target { right = mid; }
            else { return Some(mid); }
        }
        None
    }

    let sorted = vec![1, 3, 5, 7, 9, 11, 13, 15];
    println!("binary_search 7: {:?}", binary_search(&sorted, &7));
    println!("binary_search 8: {:?}", binary_search(&sorted, &8));

    // 查找插入位置
    fn lower_bound<T: Ord>(arr: &[T], target: &T) -> usize {
        let mut left = 0;
        let mut right = arr.len();
        while left < right {
            let mid = left + (right - left) / 2;
            if arr[mid] < *target { left = mid + 1; }
            else { right = mid; }
        }
        left
    }

    let arr = [1, 3, 5, 7, 9];
    println!("lower_bound of 4: {}", lower_bound(&arr, &4)); // 2
    println!("lower_bound of 5: {}", lower_bound(&arr, &5)); // 2
}
```

---

## 本章小结

| 算法 | 时间复杂度 | 空间复杂度 | 稳定性 |
|------|-----------|-----------|--------|
| 冒泡排序 | O(n²) | O(1) | 稳定 |
| 插入排序 | O(n²) | O(1) | 稳定 |
| 归并排序 | O(n log n) | O(n) | 稳定 |
| 快速排序 | O(n log n) | O(log n) | 不稳定 |
| 二分查找 | O(log n) | O(1) | — |
| BST 查找 | O(log n) 平均 | O(n) | — |

## 课后练习

1. 实现一个双向链表，支持在任意位置插入/删除（O(1) 在已知节点的情况下）
2. 实现一个 BST，支持插入、删除、查找（递归和迭代两个版本）
3. 用 BFS 实现无权图的最短路径
4. 分析代码的时间复杂度
5. 用两种方法实现字符串的全排列（递归和迭代）