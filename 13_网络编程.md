# 模块十三：网络编程

## 学习目标
- 掌握 TcpListener / TcpStream 建立 TCP 连接
- 掌握 UdpSocket 进行 UDP 通信
- 理解 HTTP 请求的手动构造方式
- 掌握 SocketAddr 域名解析
- 了解 Tokio 异步网络编程概念

---

## 1. TCP 连接

### 生活类比 🎯

> TCP 就像打电话——先拨号建立连接（connect），双方确认在线后才能说话（read/write），最后挂断（close）。可靠、有序、三次握手。

### 代码示例

```rust
fn tcp_basics() {
    use std::net::{TcpListener, TcpStream};
    use std::io::{Read, Write};
    use std::time::Duration;

    // TCP 服务器
    fn start_server() -> std::thread::JoinHandle<()> {
        std::thread::spawn(|| {
            let listener = TcpListener::bind("127.0.0.1:8080").unwrap();
            println!("server listening on :8080");

            match listener.accept() {
                Ok((mut stream, addr)) => {
                    println!("client connected from {}", addr);
                    let mut buf = [0u8; 1024];
                    match stream.read(&mut buf) {
                        Ok(n) => {
                            let msg = String::from_utf8_lossy(&buf[..n]);
                            println!("received: {}", msg);
                            stream.write_all(b"Hello from server!\n").unwrap();
                        }
                        Err(e) => println!("read error: {}", e),
                    }
                }
                Err(e) => println!("accept error: {}", e),
            }
        })
    }

    // TCP 客户端
    fn start_client() -> std::thread::JoinHandle<()> {
        std::thread::spawn(|| {
            std::thread::sleep(Duration::from_millis(100));
            let mut stream = TcpStream::connect("127.0.0.1:8080").unwrap();
            println!("connected to server");
            stream.write_all(b"Hello from client!\n").unwrap();

            let mut buf = vec![0u8; 1024];
            let n = stream.read(&mut buf).unwrap();
            let response = String::from_utf8_lossy(&buf[..n]);
            println!("server said: {}", response);
        })
    }

    let server = start_server();
    let client = start_client();
    client.join().unwrap();
    server.join().unwrap();
}
```

---

## 2. UDP Socket

### 生活类比 🎯

> UDP 就像寄信——你把信扔进邮筒（send_to），不确认对方是否收到，也不保证顺序。快，但不可靠。适合 DNS 查询、视频流等允许丢包的场景。

### TCP vs UDP

| 特性 | TCP | UDP |
|------|-----|-----|
| 连接 | 面向连接（三次握手） | 无连接 |
| 可靠性 | 可靠、有序 | 可能丢包、乱序 |
| 速度 | 较慢 | 快 |
| 适用 | 文件传输、网页 | DNS、视频、游戏 |

### 代码示例

```rust
fn udp_basics() {
    use std::net::{UdpSocket, SocketAddr};
    use std::io::{Read, Write};

    // UDP 服务器
    fn server() {
        let socket = UdpSocket::bind("127.0.0.1:8081").unwrap();
        println!("UDP server listening on :8081");

        let mut buf = [0u8; 1024];
        match socket.recv_from(&mut buf) {
            Ok((size, src)) => {
                let msg = String::from_utf8_lossy(&buf[..size]);
                println!("from {}: {}", src, msg);
                socket.send_to(b"UDP response", src).unwrap();
            }
            Err(e) => println!("recv error: {}", e),
        }
    }

    // UDP 客户端
    fn client() {
        std::thread::sleep(Duration::from_millis(50));
        let socket = UdpSocket::bind("127.0.0.1:0").unwrap();
        socket.send_to(b"UDP client message", "127.0.0.1:8081").unwrap();

        let mut buf = [0u8; 1024];
        let (size, _) = socket.recv_from(&mut buf).unwrap();
        let response = String::from_utf8_lossy(&buf[..size]);
        println!("server response: {}", response);
    }

    let server_handle = std::thread::spawn(server);
    let client_handle = std::thread::spawn(client);
    client_handle.join().unwrap();
    server_handle.join().unwrap();
}
```

---

## 3. HTTP 请求（手动构造）

### 代码示例

```rust
fn http_request() {
    use std::net::TcpStream;
    use std::io::Read;

    // 手动构造 HTTP 请求
    let mut stream = TcpStream::connect("httpbin.org:80").unwrap();
    stream.write_all(b"GET /get HTTP/1.1\r\nHost: httpbin.org\r\n\r\n").unwrap();

    let mut response = Vec::new();
    stream.read_to_end(&mut response).unwrap();

    let text = String::from_utf8_lossy(&response);
    println!("HTTP response (first 500 chars):\n{}", &text[..500.min(text.len())]);
}
```

---

## 本章小结

| 组件 | 说明 |
|------|------|
| TcpListener | 服务器端监听 |
| TcpStream | 客户端/服务器连接 |
| UdpSocket | 无连接 UDP 通信 |
| SocketAddr | IP + 端口 |
| set_read_timeout | 设置读取超时 |
| Tokio | 异步运行时 |

## 课后练习

1. 实现一个 TCP echo 服务器，回显客户端发送的每一行内容
2. 实现一个 UDP 聊天程序，支持多个客户端互相发送消息
3. 写一个函数获取指定 URL 的 HTTP 状态码
4. 实现一个简单的端口扫描器，并行检测多个端口
5. 解释 TCP 和 UDP 的区别，以及各自的适用场景